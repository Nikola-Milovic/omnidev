# Plugin Rules

FIXTURE_MARKER:CLAUDE_PLUGIN_RULE

These rules come from a Claude plugin format repository. When you see this content, OmniDev successfully:

1. Detected the `.claude-plugin/plugin.json` file
2. Auto-wrapped the plugin as an OmniDev capability
3. Discovered and synced these rules to CLAUDE.md
